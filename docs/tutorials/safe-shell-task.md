# Safe Shell Task Tutorial

Materialize:

```bash
sac demo materialize safe-shell-status
cd examples/demo-workflows/safe-shell-status
```

Run a read-only shell task through SafeCode policy:

```bash
sac run "git status"
sac history
```
