# v2.1.1: Test Build Detector

## Status
Implemented locally; tag pending.

## Base
- Depends on: `v2.1.0`
- Baseline context: `.claude/skills/current/SKILL.md`
- Runtime invariants: `.claude/skills/shared/core-runtime.md`
- New version: `v2.1.1`

## Problem
The project command detector only covers pytest and basic npm/pnpm tests. The
agent needs broader read-only command discovery for common local test, build,
and lint workflows before later planning can reason about verification options.

## Goals
- [x] Preserve existing pytest, uv pytest, npm test, and pnpm test detection.
- [x] Detect Python Ruff lint commands.
- [x] Detect npm/pnpm test, build, and lint scripts.
- [x] Detect Gradle test/build commands, preferring `./gradlew` when present.
- [x] Detect Maven test/package commands.
- [x] Detect Go test/vet commands.
- [x] Detect Cargo test/check commands.
- [x] Keep detection proposal-only; execution remains gated by shell policy.

## Testing Checklist
- [x] `PYTHONPATH=src python3 -m pytest tests/test_project_test_detector.py -q`
- [x] `PYTHONPATH=src python3 -m pytest -q`

## Acceptance Criteria
- [x] Detector covers pytest, uv, npm, pnpm, gradle, maven, go, cargo, and common lint commands.
- [x] CLI test detection continues to render policy status before execution.
- [x] `sac test run` still prefers a policy-runnable detected candidate.
