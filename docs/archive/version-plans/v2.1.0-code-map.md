# v2.1.0: Code Map

## Status
Implemented locally; tag pending.

## Base
- Depends on: `v2.0.5`
- Baseline context: `.claude/skills/current/SKILL.md`
- Runtime invariants: `.claude/skills/shared/core-runtime.md`
- New version: `v2.1.0`

## Problem
The agent needs a lightweight repository map before later versions can make
context selection and planning symbol-aware.

## Goals
- [x] Add a repo map builder under `src/safecode/index/`.
- [x] Include safe indexed files and Python class/function symbols.
- [x] Include Python imports.
- [x] Include likely test files and detected test commands.
- [x] Include project script entrypoints and Python `__main__` guards.
- [x] Add `sac index map` with summary and JSON output.
- [x] Add regression coverage for map contents and CLI output.

## Testing Checklist
- [x] `PYTHONPATH=src python3 -m pytest tests/test_repo_map.py -q`
- [x] `PYTHONPATH=src python3 -m pytest -q`

## Acceptance Criteria
- [x] Repo map outputs files, symbols, imports, tests, commands, and entrypoints.
- [x] JSON output is serializable and deterministic enough for downstream use.
- [x] Existing file and symbol indexes remain compatible.
