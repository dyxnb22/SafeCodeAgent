# v1.9.5: Agent Recovery

## Status
Implemented locally; tag pending.

## Base
- Depends on: `v1.9.4`
- Baseline context: `.claude/skills/current/SKILL.md`
- Runtime invariants: `.claude/skills/shared/core-runtime.md`
- New version: `v1.9.5`

## Problem
Interactive sessions need explicit recovery controls. Users should be able to
abort a stuck session, resume a recoverable one, and ask what failed last.

## Goals
- [x] Add `AgentSessionStore.abort(...)`.
- [x] Add `AgentSessionStore.resume(...)`.
- [x] Add `AgentSessionStore.explain_last_failure(...)`.
- [x] Add `sac agent abort --reason`.
- [x] Add `sac agent resume`.
- [x] Add `sac agent explain-last-failure`.
- [x] Fail closed when no session exists or a completed session is resumed.

## Testing Checklist
- [x] `PYTHONPATH=src python3 -m pytest tests/test_agent_session.py -q`
- [x] `PYTHONPATH=src python3 -m pytest -q`

## Acceptance Criteria
- [x] Aborting stores status `aborted` and a reason.
- [x] Explain-last-failure returns the stored reason.
- [x] Resuming an aborted session returns it to `active`.
- [x] Completed sessions cannot be resumed.
