# v2.0.5: MVP Docs

## Status
Implemented locally; tag pending.

## Base
- Depends on: `v2.0.4`
- Baseline context: `.claude/skills/current/SKILL.md`
- Runtime invariants: `.claude/skills/shared/core-runtime.md`
- New version: `v2.0.5`

## Problem
The v2.0 MVP has the core local workflow, but a new user needs one coherent
path from install to model configuration, first task, safety review, test run,
apply, and rollback.

## Goals
- [x] Add an MVP user guide covering install, model configuration, first task,
      safety model, and rollback.
- [x] Link the guide from README.
- [x] Document the exact real-LLM network policy requirements.
- [x] Use the v2.0.4 failing-test repair demo as the first realistic task.
- [x] Add a regression test so the key guide sections and commands remain
      present.

## Testing Checklist
- [x] `PYTHONPATH=src python3 -m pytest tests/test_mvp_docs.py -q`
- [x] `PYTHONPATH=src python3 -m pytest -q`

## Acceptance Criteria
- [x] Install instructions include local development and local tool install.
- [x] Model configuration documents mock and OpenAI-compatible modes.
- [x] First task walks through test failure, edit, apply, test pass, and history.
- [x] Safety model explains patch review, shell policy, config boundaries, and
      network defaults.
- [x] Rollback instructions explain expected restored state.
