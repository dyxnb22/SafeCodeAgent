# v1.9.1: Agent Step Command

## Status
Implemented locally; tag pending.

## Base
- Depends on: `v1.9.0`
- Baseline context: `.claude/skills/current/SKILL.md`
- Runtime invariants: `.claude/skills/shared/core-runtime.md`
- New version: `v1.9.1`

## Problem
v1.9.0 can persist session state, but there is no agent-loop primitive that
advances a goal in small, auditable increments.

## Goals
- [x] Add `AgentLoop.step(...)`.
- [x] Add `sac agent step "goal"`.
- [x] Create a session from the supplied goal when none exists.
- [x] Advance exactly one plan item per invocation.
- [x] Record only a read-only pending action for now.
- [x] Fail closed when no session exists and no goal is provided.

## Testing Checklist
- [x] `PYTHONPATH=src python3 -m pytest tests/test_agent_session.py -q`
- [x] `PYTHONPATH=src python3 -m pytest -q`

## Acceptance Criteria
- [x] `sac agent step "goal"` creates `.sac/session.json`.
- [x] Re-running `sac agent step` advances the step counter by one.
- [x] The pending action is typed as read-only.
- [x] No patch, shell, MCP, or sandbox execution occurs.
