# v3.9.1 — pipx/Brew Docs + Versioning Policy

## Summary

Documents supported install commands including TestPyPI/pipx rehearsal, offline
wheel install recipe, and a brew strategy decision. Adds `docs/versioning-policy.md`
with patch/minor/major semantics and the v4.0 contract churn budget.

## T-3.9.1-A: pipx/Brew Docs

**Changes:**
- `docs/install-update.md`: already updated in v3.9.0 with TestPyPI rehearsal
  and pipx install commands. v3.9.1 verifies these are present via tests.
- `docs/versioning-policy.md`: "Brew Distribution Strategy" section documents
  the decision — **defer** — with rationale: no production PyPI wheel has shipped
  yet; a tap will be set up once the first production release is confirmed (target
  v3.10.x or v4.0 with a passing live-provider CI train).
- `README.md`: link to `docs/versioning-policy.md` added alongside existing
  public-contracts and providers links.
- `tests/test_install_docs.py` (new): 18 tests across 6 classes enforcing that
  all required install command fragments are present in `docs/install-update.md`
  from a fixture list — no live network.

## T-3.99.0-B: Versioning Policy Doc (landed early)

**Decision: landed at v3.9.1** per task spec (T-3.99.0-B landed early).

**Changes:**
- `docs/versioning-policy.md` (new): documents patch/minor/major semantics,
  stable vs experimental surfaces, v4.0 churn budget (at most two new stable
  contracts; zero breaking changes to v3.0 contracts), and brew strategy.
- `tests/test_versioning_policy_doc.py` (new): 12 tests across 6 classes
  verifying all required policy sections are present and README links the doc.
