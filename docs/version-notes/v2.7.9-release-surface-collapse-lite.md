# v2.7.9 release-surface-collapse-lite

## Goal
Continue flattening the release surface without breaking command compatibility: make signoff and checklist explicitly label themselves in their runtime output.

## Changes
- `src/safecode/release/signoff.py`: `render_release_signoff()` prepends `[deprecated] Use sac release preflight instead.` notice.
- `src/safecode/release/checklist.py`: `render_release_checklist()` prepends `[planning helper] ... not a release gate` notice after the heading.
- `docs/install-update.md`: main flow code block adds `sac release sync-versions-json` step; preflight description updated to mention versions governance.
- Both commands remain callable; no command removed.

## Tests
- `tests/test_release_surface_collapse.py`: 10 new tests covering deprecated/planning helper notices in output and help, docs main flow contains preflight and sync-versions-json but not signoff.

## Safety
No functional changes to release logic. Notices are informational only.
