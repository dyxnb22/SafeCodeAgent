# v2.6.19 — Policy Audit Command

## Summary

Adds `sac config policy-audit`, a read-only check for policy names, legacy
aliases, unknown project/env policies, and safety invariants across presets.

## What changed

- `src/safecode/policy/audit.py` (new): `audit_policy()` and
  `render_policy_audit()`.
- `src/safecode/cli_project.py`: adds `sac config policy-audit`.
- `tests/test_policy_audit.py`: focused helper and CLI coverage.

## Verification

```bash
PYTHONPATH=src python3 -m pytest tests/test_policy_audit.py tests/test_policy_presets.py tests/test_migration_hardening.py -q
PYTHONPATH=src python3 -m pytest -q
```
