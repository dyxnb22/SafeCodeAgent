# v3.11.1 - policy-diff

## Summary

- T-3.11.1-A policy-diff: added `sac config diff --against strict|balanced|experimental`.
- The command compares effective config to the named preset across policy knobs only: shell confirmation/blocking settings, allowed commands, sandbox root/network settings, and hook medium-risk behavior.
- `--json` uses the existing `CLIJSONResponse` envelope with deterministic sorted keys.

## Safety Notes

- Preset definitions are unchanged.
- Output is deterministic and excludes LLM provider/base URL or other secret-bearing config.
- Unknown presets fail closed.

## Validation

- Targeted: `PYTHONPATH=src python3 -m pytest tests/test_policy_diff.py -q` -> 5 passed.
- Full suite: `PYTHONPATH=src python3 -m pytest -q` -> 3759 passed, 2 skipped.
