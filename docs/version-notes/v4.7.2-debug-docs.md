# v4.7.2 Debug Docs

Status: implemented.

## Scope

- T-4.7.2-A v4.7-docs-cut

## Implementation Notes

- README Core Commands now documents experimental `sac debug last-failure`, `sac debug bundle`, and `sac audit query`.
- MVP guide now documents the v4.7 debug workflow: inspect last failure, create a redacted debug bundle, and query audit events by task/type/date.
- Troubleshooting now includes a v4.7 experimental failure taxonomy section.
- Every category has a plain-English meaning, likely cause, and suggested command matching `SUGGESTED_COMMAND_BY_CATEGORY`.
- Docs guard tests verify category docs match code and documented debug/audit commands exist.

## Validation

- `PYTHONPATH=src python3 -m pytest tests/test_public_contract_snapshots.py tests/test_versioning_policy_doc.py tests/test_mvp_docs.py -q` -> 125 passed.
- `PYTHONPATH=src python3 -m pytest -q` -> 4138 passed, 2 skipped.
- `PYTHONPATH=src python3 -m safecode.cli release preflight` -> blocked before commit only by the expected uncommitted-change gate; rerun after commit/tag for release signoff.

## Contract Notes

All v4.7 surfaces are EXPERIMENTAL. No stable public contract was promoted.
