# v4.25.0: checkpoint-integrity

## Base
- Depends on: `v4.24.2`
- Plan: `docs/version-plans/v4.20-to-v5.0-product-roadmap.md` (v4.25.0 section)

## Completion Notes
- `src/safecode/checkpoint/models.py`:
  - `CheckpointFileOperation.backup_sha256: str | None = None` — new optional field; default None for backward compat.
- `src/safecode/checkpoint/manager.py`:
  - `CheckpointIntegrityError(RuntimeError)`: structured exception with `path`, `checkpoint_id`, `expected`, `actual` attributes; message includes truncated hashes.
  - `_sha256_of_file(path)`: streaming sha256 using 64 KB chunks.
  - `create()` B13 fix: after `shutil.copy2`, computes and stores `backup_sha256` in each `CheckpointFileOperation`.
  - `_restore_checkpoint()` B13 fix: two-pass approach — pre-flight integrity check over all operations before touching any target file; if any sha256 mismatches, raises `CheckpointIntegrityError` without modifying targets. Old checkpoints with `backup_sha256=None` skip verification (backward compat).

## Tests
- `tests/test_checkpoint.py` extended (7 total, all pass):
  - `test_create_stores_sha256_in_file_operation`: sha256 stored and correct.
  - `test_restore_passes_when_sha256_matches`: clean round-trip.
  - `test_restore_raises_integrity_error_on_corrupt_backup`: corruption detected, target NOT modified.
  - `test_restore_skips_sha256_check_for_old_checkpoint`: backward compat with sha256=None.
  - `test_checkpoint_integrity_error_attributes`: exception attributes and message.

## Tag
- `v4.25.0`
