# v3.10.1 — ci-gates-live-provider

## Summary

- T-3.10.1-A loop-eval-blocking: Promotion from advisory to blocking **deferred**. No clean CI train for loop-eval has been recorded in this repository. The `loop-eval` CI job remains `continue-on-error: true`. Extended `tests/test_eval_loop_mode_ci_gate.py` with 4 new tests encoding the current truthful state: job is still advisory at v3.10.1; no secrets required; scripted loop mode exits 0 locally (confirming readiness for future promotion when evidence is available).
- T-3.10.1-B live-provider-lane-record: One full v3.10.x release train of live-provider lane status recorded in `docs/providers.md` (Release Train History table). Lane remains advisory; promotion criteria documented (one clean live-provider CI train required). Credential handling unchanged. Extended `tests/test_ci_live_lane.py` with 7 new tests verifying advisory status, providers.md documentation, version note existence, and credential isolation.

## Loop-Eval Promotion Deferral

Promotion is deferred because:
1. No recorded CI run confirms a clean live train in this repository.
2. The scripted loop mode exits 0 locally (6/6 fixtures pass) — the technical
   readiness condition is met.
3. Promotion requires external CI evidence (GitHub Actions run history), not just
   local test results.

When evidence is available, remove `continue-on-error: true` from the `loop-eval`
job in `.github/workflows/ci.yml` and update this note.

## Live-Provider Lane Status

Lane remains advisory at v3.10.1. See `docs/providers.md` → "Release Train History".

## Claims vs Implementation

| Claim | Status |
|---|---|
| loop-eval job present in CI | ✅ |
| loop-eval advisory (continue-on-error: true) | ✅ documented truthfully |
| Promotion deferred with documented reason | ✅ |
| Live-provider lane advisory | ✅ |
| Release train history recorded | ✅ (docs/providers.md) |
| No credential handling changes | ✅ |
| No inline secrets | ✅ |
