# v2.3.4 — Onboarding Examples

## Summary

Adds guided onboarding material for four common SafeCode workflows.

## Implementation

- `docs/tutorials/` includes bug fix, feature edit, docs edit, and safe shell task guides.
- `examples/README.md` points users to built-in demo workflows.
- `DemoWorkflowSuite` now includes `safe-shell-status` for a read-only shell command workflow.

## Verification

```bash
PYTHONPATH=src python3 -m pytest tests/test_onboarding_examples.py -q
PYTHONPATH=src python3 -m pytest -q
```
