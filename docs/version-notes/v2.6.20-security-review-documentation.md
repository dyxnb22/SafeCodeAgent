# v2.6.20 — Security Review Documentation

## Summary

Adds a product security review for the v2.6 hardening surface, covering config,
sandbox defaults, hooks, release gates, and trust boundaries.

## What changed

- `docs/security/product-security-review-v2.6.md` (new): product security
  review and checklist.
- `tests/test_security_review_docs.py`: verifies the review covers the required
  sections and key boundary statements.

## Verification

```bash
PYTHONPATH=src python3 -m pytest tests/test_security_review_docs.py -q
PYTHONPATH=src python3 -m pytest -q
```
