# v6.6.3 — docs-cleanup

Current docs and guard tests are aligned with the actual v6 stable-contract
state.

Changes:
- README and MVP guide: Trust Modes now point to Stable Contract #18, GitHub
  write workflow points to Stable Contract #20, and stale "no v5.0 scheduled"
  wording is removed.
- `docs/public-contracts.md`: Section 20 remains GitHub PR Workflow; Section 21
  is Project Memory Store Shape candidate; the "not stable" list no longer
  claims GitHub write tools are experimental.
- `docs/security/threat-model-v3.6.md`: GitHub write tools now reference Stable
  Contract #20.
- README provider, project-memory, and semantic-search material now points to
  canonical docs instead of duplicating every detail.
- `tests/test_mvp_docs.py`: GitHub docs guard checks the current stable-contract
  wording instead of an old v4.24 marker.
- `src/safecode/agent/loop.py` and `src/safecode/cli_shell.py`: reduced noisy
  historical version comments while preserving why-oriented comments.

Validation:
- `PYTHONPATH=src python3 -m pytest tests/test_mvp_docs.py tests/test_landing_docs.py tests/test_docs_claims_guard.py tests/test_github_pr_contract.py tests/test_v6_contract_candidates.py -q`
