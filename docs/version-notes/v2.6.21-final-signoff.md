# v2.6.21 — v2.6 Final Signoff

## Summary

Closes the v2.6 product-hardening line with a final signoff command, updated
implementation matrix, SKILL baseline, release preflight, and exact tag
verification.

## What changed

- `src/safecode/release/signoff.py` (new): `run_release_signoff()` and
  `render_release_signoff()`.
- `src/safecode/cli_ops.py`: adds `sac release signoff`.
- `docs/version_implementation_matrix.md`: updates the v2.6.x matrix through
  v2.6.21.
- `tests/test_release_signoff.py`: focused signoff result/render coverage.

## Verification

```bash
PYTHONPATH=src python3 -m pytest tests/test_release_signoff.py tests/test_release_preflight.py -q
PYTHONPATH=src python3 -m pytest -q
PYTHONPATH=src python3 -m safecode.cli release preflight
git describe --exact-match --tags HEAD
```
