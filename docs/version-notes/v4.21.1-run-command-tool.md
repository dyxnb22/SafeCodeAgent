# v4.21.1: run-command-tool

## Base
- Depends on: `v4.21.0`
- Plan: `docs/version-plans/v4.20-to-v5.0-product-roadmap.md` (v4.21.1 section)

## Completion Notes
- `src/safecode/agent/command_tool.py` (new): `run_command` native tool.
  - Routes through existing `ShellRunner`/policy stack (same risk classification and
    high-risk block rules as `sac run`).
  - `requires_approval=False`; `audit_event_type="tool_call_command"`.
  - High-risk or policy-blocked commands return `status="blocked"`.
  - `cwd` validated to stay within project root.
  - `timeout_seconds` parameter (default 60s).
  - `register_command_tool(dispatcher, project_root)` wires the tool.
- No stable contract promoted; no new safety gate added beyond existing ShellRunner.

## Tests
- `tests/test_run_command_tool.py`: 10 tests (spec, missing command, cwd-outside-root,
  successful command, high-risk block, nonzero exit, cwd inside project, duration
  metadata, register integration, dispatch integration). All ShellRunner calls mocked.
- Full suite: 4982 passed, 4 skipped.

## Tag
- `v4.21.1`
