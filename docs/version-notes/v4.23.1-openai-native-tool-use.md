# v4.23.1: openai-native-tool-use

## Base
- Depends on: `v4.23.0`
- Plan: `docs/version-plans/v4.20-to-v5.0-product-roadmap.md` (v4.23.1 section)

## Completion Notes
- `src/safecode/llm/openai_client.py`:
  - `_native_spec_to_openai()`: converts `NativeToolSpec` to OpenAI function calling format (`{"type":"function","function":{...}}`).
  - `choose_tool_native(goal, context, tool_specs, *, step)`: sends `tools` parameter to OpenAI; maps `tool_calls` response to `list[AgentNativeToolCallResponse]`; wraps text as `AgentStopForUserResponse`; B1 fix — returns `RecoverableContractFailure` on empty choices.
  - B1 fix: `_chat()` bounds-checks `choices` before indexing; returns `""` on empty/missing choices instead of raising `IndexError`. Callers get `RecoverableContractFailure` from `validate_provider_json`.
  - Import: `TYPE_CHECKING`, `Any`, `AgentNativeToolCallResponse`, `NativeToolSpec` added.
  - DeepSeek inherits via the OpenAI-compatible path.
- `src/safecode/llm/retry.py`:
  - B12 fix: `_sanitize_retry_reason(reason)` strips `https?://...` URLs → `[URL]` using `re` module; applies `redact_secrets()` for secret redaction. Applied to all `log_fn` calls in `retry_call()`.
  - Import: `re` added.

## Tests
- `tests/test_openai_native_tool_use.py` (14 tests, all pass):
  - `TestNativeSpecToOpenAI` (3): format conversion, type injection, schema preservation
  - `TestB1EmptyChoicesBoundsCheck` (3): B1 fix — missing/empty choices, RCF from choose_tool_native
  - `TestChooseToolNativeOpenAI` (3): tool_calls mapped, text→stop_for_user, invalid JSON args
  - `TestB12RetrySanitization` (5): URL replacement, multiple URLs, no-URL passthrough, http, log_fn integration
- `tests/test_llm_retry.py`: 14 existing tests all pass (no regressions).

## Tag
- `v4.23.1`
