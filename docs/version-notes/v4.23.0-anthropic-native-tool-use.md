# v4.23.0: anthropic-native-tool-use

## Base
- Depends on: `v4.22.2`
- Plan: `docs/version-plans/v4.20-to-v5.0-product-roadmap.md` (v4.23.0 section)

## Completion Notes
- `src/safecode/llm/stream.py`: `StreamTimeoutError(StreamError)` added (B3 fix).
- `src/safecode/llm/anthropic_client.py`:
  - `_native_spec_to_anthropic()`: converts `NativeToolSpec` to Anthropic `tools` parameter format.
  - `_extract_text()`: B2 fix — validates content list non-empty before access; returns `""` on missing/empty/non-list content instead of crashing.
  - `_extract_native_result()`: extracts `tool_use` blocks → `list[AgentNativeToolCallResponse]`, or text → `str`, or `RecoverableContractFailure` on malformed/empty content (B2 fix).
  - `choose_tool_native(goal, context, tool_specs, *, step)`: sends Anthropic native tool use; maps `tool_use` blocks to `AgentNativeToolCallResponse`; wraps text response as `AgentStopForUserResponse`.
  - `_messages_with_tools(system, user, tools)`: internal method for tool-bearing API calls.
  - `_http_stream_lines_anthropic()`: B3 fix — timeout reduced to 30s, `socket.timeout` caught and re-raised as `StreamTimeoutError`.
  - Import: `socket` added; `StreamTimeoutError` imported.
- `src/safecode/doctor.py`:
  - `_live_anthropic_ping(api_key, *, timeout)`: static method; performs `GET /v1/models` with `x-api-key` header; PASS/FAIL/SKIP diagnostic (B16 fix).
  - `run_diagnostics(live=True)`: when `config.llm.provider == "anthropic"`, appends `_live_anthropic_ping()` result after the generic provider ping.

## Tests
- `tests/test_anthropic_native_tool_use.py` (25 tests, all pass):
  - `TestNativeSpecToAnthropic` (3): format conversion, type injection
  - `TestExtractTextB2Fix` (5): B2 fix coverage — missing/None/empty content, no-text-blocks
  - `TestExtractNativeResult` (7): tool_use blocks, text fallback, B2 RCF paths
  - `TestStreamTimeoutError` (3): B3 — subclass, raise-as-StreamError, message
  - `TestChooseToolNativeAnthropic` (3): tool calls returned, stop_for_user fallback, B2 RCF
  - `TestLiveAnthropicPingB16` (4): B16 — PASS/FAIL/SKIP variants

## Tag
- `v4.23.0`
