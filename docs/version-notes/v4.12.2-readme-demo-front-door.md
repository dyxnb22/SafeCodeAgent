# v4.12.2 - README Demo Front Door

Moves the resume-MVP demo to the README front door with a concise command path, links to `examples/fastapi-todo/`, the recorded transcript, and `docs/tutorials/from-task-to-tested-commit.md`. The MVP guide and agent-run first-hour tutorial now point users to the mock-first demo before live-provider work.

Validation: `PYTHONPATH=src python3 -m pytest -q tests/test_docs_demo_section.py tests/test_docs_claims_guard.py`; full `PYTHONPATH=src python3 -m pytest -q`.

Safety and contract note: docs explicitly avoid claims of auto-apply, auto-commit, live-provider requirement, IDE requirement, push, or PR automation. No stable contract is promoted.
