# v3.9.3 — v3.9.x Docs Cut

## Summary

Documentation-only release: aligns README, `docs/install-update.md`, and the
version matrix with the actual v3.9.x implementation. No runtime feature work.

## T-3.9.3-A: v3.9 Docs Cut

**README changes:**
- "Install" section updated: local dev, pipx (once on PyPI), TestPyPI rehearsal
  install, offline wheel recipe. Removes stale `uv tool install .` as primary path.
- "Release Flow" section updated: adds `sac release sync-versions-json` step,
  tag-move pattern, TestPyPI rehearsal commands, and production publish gate.
  Removes stale v2.6.13 example.
- New "IDE and TUI Status (v3.9.x)" section: describes VS Code extension source
  location, approval modal, VSIX deferral note, and TUI frozen-experimental status.

**`docs/install-update.md` changes (accumulated from v3.9.0–v3.9.2):**
- "Release Signing" section: detached cosign/gpg mechanism, not Sigstore Rekor.
- "TestPyPI Rehearsal" section: dry-run, real upload, pipx install from TestPyPI.
- "pipx Install" section: production pipx, upgrade, offline wheel recipe.

**Version matrix:**
- Rows for v3.9.0, v3.9.1, v3.9.2, v3.9.3 added.

**SKILL.md:**
- Current baseline updated to v3.9.3.

## Claims vs Implementation Check

| Claim | Status |
|---|---|
| `--sign` produces detached cosign/gpg signature | ✅ implemented; docs match |
| `--repository test-pypi` for TestPyPI upload | ✅ implemented; real upload requires `SAFECODE_PUBLISH=1` |
| pipx install from PyPI | ⚠️ deferred — no production wheel on PyPI yet |
| pipx install from TestPyPI (rehearsal) | ✅ documented; no real run (no published wheel) |
| Offline wheel recipe | ✅ documented; `uv build` + `pipx install dist/*.whl` |
| Brew tap/formula | ⚠️ deferred — no PyPI wheel yet; documented in `docs/versioning-policy.md` |
| VS Code extension VSIX built | ⚠️ deferred — source written; VSIX requires tsc; smoke procedure documented |
| TUI Textual upgrade | ❌ decided against — frozen at v3.5.2 Rich-based behavior |
| TUI frozen experimental labeled | ✅ in `docs/public-contracts.md` |
