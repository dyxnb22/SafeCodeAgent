# v2.6.13 — Release Workflow Documentation

## Summary

Documents the intended release flow for humans and future agents: bump, test,
commit, tag, verify exact tag, and run the release gates.

## What changed

- `README.md`: adds a release flow section with the canonical command order.
- `docs/install-update.md`: expands release checks into the full release
  workflow.
- `src/safecode/release/docs_guard.py`: recognizes `sac release bump` and
  `sac release preflight` as release commands for docs finalization checks.
- `.claude/skills/current/SKILL.md`: updates the baseline to v2.6.13.

## Verification

```bash
PYTHONPATH=src python3 -m pytest tests/test_release_docs_guard.py tests/test_release_smoke.py tests/test_release_preflight.py -q
PYTHONPATH=src python3 -m pytest -q
```
