# v6.6.2 — real-world-demo

Adds a real-world demo scaffold based on a public Arrow parser exception
boundary issue.

Changes:
- `examples/real-world-demo/`: README, reproducer setup script, demonstration
  transcript, and SWEBenchTask-shaped fixture.
- `tests/eval_fixtures/swebench_lite/real-world-arrow-parser-error.json`: inline
  standard-library adaptation of the Arrow ParserError boundary bug.
- `tests/snapshots/swebench_lite/latest.json`: mock-provider baseline updated
  to honest 0/8.
- README and eval docs link the real-world example and clarify the 7 synthetic
  + 1 real-world-inline benchmark scope.

Validation:
- `PYTHONPATH=src python3 -m pytest tests/test_eval_swebench_adapter.py -q`
- `PYTHONPATH=src python3 -m safecode.cli eval --mode swebench-lite --suite tests/eval_fixtures/swebench_lite`
