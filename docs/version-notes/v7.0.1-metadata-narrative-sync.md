# v7.0.1 — Metadata and Narrative Sync

v7.0.1 is a documentation and release-metadata alignment release after the
v7.0.0 stable contract cut.

## Changed

- Bumped package/runtime metadata to `7.0.1`.
- Updated README claims from the stale 28-fixture / 21-contract wording to the
  current 36-fixture default live eval suite and 23 stable contracts.
- Clarified that the committed DeepSeek v4-flash artifact covers the earlier
  28-fixture run while the default ratchet baseline now covers 36 fixtures.
- Updated docs entry points, project status, version summary, version-plan
  index, `.claude/versions.json`, and `.claude/skills/current/SKILL.md`.
- Added the active v7.0.x productization follow-up plan and guarded it in docs
  governance tests.

## Stable Contracts

- No runtime behavior changed.
- No stable contract changed.
- v7.0.0 remains the current stable-contract cut.

## Validation

```bash
uv run pytest tests/test_docs_governance.py -q
```
