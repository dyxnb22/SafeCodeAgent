# v3.7.3 — v3.7.x docs cut

## Summary

- **T-3.7.3-A v3.7-docs-cut** (`README.md`, `docs/mvp-user-guide.md`): documentation updated to
  reflect all v3.7.x features shipped in v3.7.0–v3.7.2.

  **README.md** Core Commands section updated:
  - `sac setup --wizard` added with description.
  - `sac quickstart` description updated to reflect stack detection.
  - `sac fix` and `sac fix --test-command` added.
  - `--json` usage section added showing `sac fix --json`, `sac edit ... --json`, `sac ask ... --json`.

  **docs/mvp-user-guide.md** updated:
  - Intro updated to reference v3.7.x path.
  - Quickstart description updated: step 2 now documents stack detection (Python/TypeScript/Go/Rust).
  - New "Interactive setup wizard" subsection under Quickstart documents `sac setup --wizard`,
    TTY vs. non-TTY behaviour, and safety invariants (cannot lower policy, mock→live confirmation,
    network double-confirm).
  - New "Fixing failing tests with sac fix" section documents the full `sac fix` cycle,
    `--test-command` override, `--json` flag, and already-passing behavior.
  - New "Machine-readable output" section documents stable JSON envelope contract (Section 11
    of `docs/public-contracts.md`) and the `"error" in json.loads(output)` idiom.

## Safety invariants preserved

- No runtime code changed.
- No contract claims added that are not already in `docs/public-contracts.md`.
- Experimental surfaces (TTY wizard, progress indicators) remain labeled experimental or
  omitted from claim-level contract text.
