# v2.6.16 — Release Checklist Upgrade

## Summary

Upgrades `sac release checklist` so it reflects the current release workflow:
bump, docs, focused tests, full regression, release gates, commit, tag, and
exact-tag verification.

## What changed

- `src/safecode/release/checklist.py`: expanded checklist sections for version
  docs, verification, and git release actions.
- `tests/test_release_checklist_upgrade.py`: focused renderer and CLI coverage.

## Verification

```bash
PYTHONPATH=src python3 -m pytest tests/test_release_checklist_upgrade.py tests/test_runtime_extensions.py -q
PYTHONPATH=src python3 -m pytest -q
```
