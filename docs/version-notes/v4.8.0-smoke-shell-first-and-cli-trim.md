# v4.8.0 — smoke-shell-first-and-cli-trim

## Summary

v4.8.0 closes the final v4.x reliability batch with two tasks:

- **T-4.8.0-A smoke-shell-first**: New `src/safecode/cli_smoke.py` with `sac smoke shell-first` — a deterministic 8-scenario workflow smoke suite running exclusively under mock provider (no real network, no real LLM, no mutation of the real repo). Target wall time: ≤60 s.
- **T-4.8.0-B cli-surface-trim**: Root `sac --help` trimmed to exactly 17 visible daily-loop commands. `memory` promoted from hidden to visible. All other advanced/internal commands remain callable.

## New files

- `src/safecode/cli_smoke.py` — smoke suite Typer app and 8 scenario functions
- `tests/test_smoke_shell_first.py` — 31 tests for smoke registry, CLI invocation, JSON output, and per-scenario correctness
- `tests/test_cli_help_surface_v4_8.py` — 62 tests for v4.8 CLI surface (visible count, hidden/callable invariants)
- `docs/version-notes/v4.8.0-smoke-shell-first-and-cli-trim.md` (this file)

## Modified files

- `src/safecode/cli.py` — register `smoke_app` (hidden), make `memory_app` visible, hide 18 other advanced groups
- `src/safecode/cli_core.py` — `history` command hidden
- `src/safecode/cli_ops.py` — `eval` command hidden
- `src/safecode/cli_commit.py` — `branch` and `diff` commands hidden
- `tests/test_cli_help_surface_trim.py` — updated: removed `memory` from hidden list (now visible)
- `pyproject.toml`, `src/safecode/__init__.py`, `uv.lock` — bumped to 4.8.0
- `.claude/versions.json` — updated to v4.8.0
- `.claude/skills/current/SKILL.md` — baseline updated to v4.8.0
- `docs/version_implementation_matrix.md` — v4.8.0 row added

## Smoke scenarios (8)

1. `docs-edit-task` — task creation + pending patch lifecycle
2. `failing-test-repair-with-fix-watch` — mock fix flow produces pending patch
3. `command-profile-detection` — pytest detected in Python pyproject.toml project
4. `dirty-tree-refusal` — `dirty_tree_guard` detects unrelated staged files
5. `rollback-after-commit-warn` — `CheckpointManager` finds checkpoint metadata
6. `resume-after-sigint` — interrupted task found by TaskStore
7. `debug-bundle-redaction` — bundle produced, ≤5 MiB, no project source
8. `pinned-files-in-context` — pinned file appears in ContextSelector results

## Root help visible surface (17 commands)

setup, quickstart, status, task, ask, edit, fix, apply, rollback, run, commit, profile, resume, memory, debug, doctor, version

## Hidden but callable (selection)

queue, progress, audit, hooks, export, tui, ide, index, skills, tools, subagent, eval, context, mcp, sandbox, trust, smoke, release, logs, report, agent, test, demo, config, history, branch, diff, rules

## CI lane command

```
PYTHONPATH=src python3 -m pytest tests/test_smoke_shell_first.py tests/test_cli_help_surface_v4_8.py -q
```

## Safety invariants preserved

- No diff review, checkpoint, audit, rollback, policy, or sandbox gate bypassed.
- Smoke scenarios use mock provider only — no real LLM, no real network.
- No stable public contract promoted.
- All v4.8 additions are experimental.
- Hidden commands remain fully callable.
