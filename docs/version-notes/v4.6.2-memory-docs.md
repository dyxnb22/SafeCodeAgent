# v4.6.2 - memory docs

## Status

Implemented. All v4.6 memory surfaces remain EXPERIMENTAL. No stable public contracts were promoted.

## What changed

- README Core Commands now documents `sac memory show`, `sac memory pin`, `sac memory unpin`, `sac memory add-note`, and `sac memory clear`.
- MVP guide documents the unified v4.6 memory layout.
- MVP guide documents project notes, task notes, pinned files, context quota behavior, and recent failures helping `sac fix`.
- Troubleshooting documents pinned missing files, pinned outside-root refusal, memory secret rejection, and stale recent-failure context.
- Docs guard tests verify documented v4.6 memory commands and options exist.

## Safety notes

- All v4.6 surfaces are marked EXPERIMENTAL.
- No stable contract promotion was made.
- Docs mention only commands implemented in the CLI.
- No `PatchProposal` or `AuditEvent` field sets changed.

## Validation

```bash
PYTHONPATH=src python3 -m pytest tests/test_public_contract_snapshots.py tests/test_versioning_policy_doc.py -q
PYTHONPATH=src python3 -m pytest -q
PYTHONPATH=src python3 -m safecode.cli release preflight
```
