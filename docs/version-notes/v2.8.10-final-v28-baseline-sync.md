# v2.8.10 — Final v2.8 Baseline Sync

## Summary

Metadata-only release marker that establishes the final v2.8.x baseline after
the v2.8.6 and v2.8.7 cleanup work landed on top of the already-tagged
v2.8.8/v2.8.9 commits.

No runtime behavior changes are included in this version.

## Changed Files

- `pyproject.toml` — package version synchronized to `2.8.10`
- `src/safecode/__init__.py` — runtime version synchronized to `2.8.10`
- `uv.lock` — locked project version synchronized to `2.8.10`
- `.claude/versions.json` — current implemented tag and local version synchronized to `v2.8.10`
- `.claude/skills/current/SKILL.md` — current baseline updated to `v2.8.10`
- `docs/version_implementation_matrix.md` — v2.8.10 completion row added

## Acceptance

```bash
PYTHONPATH=src python3 -m pytest -q
PYTHONPATH=src python3 -m safecode.cli release preflight
```
