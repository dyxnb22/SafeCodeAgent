# v4.3.2 — Fix Watch Docs

## Summary

v4.3.2 cuts the v4.3 documentation for the experimental reliable test-fix
loop.

No stable contract changes. `sac fix --watch` never auto-applies.

## Tasks

### T-4.3.2-A v4.3-docs-cut

- `README.md` Core Commands now documents:
  - `sac fix --watch`
  - `--max-iterations`
  - `--timeout-seconds`
  - `--rerun-suite`
- `docs/mvp-user-guide.md` now documents the approval-gated loop:
  `sac fix --watch` -> review -> `sac apply` -> `sac fix --watch`.
- `docs/troubleshooting.md` now covers:
  - `loop_no_progress`
  - `command_timeout`
  - max iterations reached
  - blocked suite command
  - missing profile suite
- `tests/test_mvp_docs.py` now checks the documented v4.3 commands and options
  exist on the actual `sac fix` CLI.

## Validation

- Docs guard tests updated.
- Full validation run as part of release train.

## Stability

All v4.3 surfaces are EXPERIMENTAL. No stable contract promoted or changed.
