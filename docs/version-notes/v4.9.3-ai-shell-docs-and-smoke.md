# v4.9.3 — ai-shell-docs-and-smoke

## Summary

v4.9.3 delivers the documentation and smoke-test finalization for the v4.9 AI Shell MVP train:

- **T-4.9.3-A docs-and-smoke**: AI shell tutorial (`docs/tutorials/ai-shell-first-hour.md`), README/MVP guide/troubleshooting/public-contracts/versioning-policy updated with v4.9 EXPERIMENTAL surfaces; `sac smoke ai-shell` with 6 deterministic scenarios; docs claims guard (`tests/test_ai_shell_docs_claims.py`). v4.9 adds zero stable contracts. No v5 scheduled.

## New/modified files

- `docs/tutorials/ai-shell-first-hour.md` — AI shell first-hour tutorial (EXPERIMENTAL label, no auto-apply, no RAG, no v5 promise)
- `docs/mvp-user-guide.md` — "AI Shell (v4.9, EXPERIMENTAL)" section added
- `docs/troubleshooting.md` — "AI Shell Issues (v4.9, EXPERIMENTAL)" section added
- `docs/public-contracts.md` — "v4.9.x AI Shell MVP Contract Summary" section added (zero new stable contracts)
- `docs/versioning-policy.md` — "v4.9.x AI Shell Train" section and v4.9.3 policy changelog entry added
- `docs/version_implementation_matrix.md` — v4.9.x table added with v4.9.0–v4.9.3 rows
- `README.md` — "AI Shell (v4.9, EXPERIMENTAL)" block added; AI shell tutorial link added
- `src/safecode/cli_smoke.py` — `sac smoke ai-shell` with 6 deterministic scenarios
- `tests/test_smoke_ai_shell.py` — 11 smoke tests
- `tests/test_ai_shell_docs_claims.py` — 35 docs claims guard tests
- `docs/version-notes/v4.9.3-ai-shell-docs-and-smoke.md` (this file)
- `pyproject.toml`, `src/safecode/__init__.py` — bumped to 4.9.3
- `.claude/versions.json` — updated to v4.9.3
- `.claude/skills/current/SKILL.md` — baseline updated to v4.9.3

## Tutorial contents

- Start the shell (`sac shell`)
- Understand the project (`/overview`)
- Find a bug (`ask` natural language)
- Propose a fix (`fix` natural language, explicit approval required)
- Run checks (`/run tests`)
- Apply patch (`/apply` — requires `y` confirmation in TTY; prints instructions in non-TTY)
- Roll back (`sac rollback`)
- Commit (`/commit` — requires `y` confirmation)
- Slash command reference table
- Non-TTY/script mode section
- Session continuity section
- Safety guarantees section
- "What the shell does not do" section (no RAG, no v5, etc.)

## Smoke scenarios

- `session-create-and-persist` — new session is created and saved
- `corrupt-session-fail-safe` — corrupt JSON returns None, shell creates fresh session
- `intent-router-read-only` — read-only intents produce responses without confirmation
- `overview-builds-for-python-project` — project overview aggregates signals
- `non-tty-no-auto-apply` — non-TTY apply prints instructions, never executes
- `shell-loop-deterministic` — REPL loop with mock provider exits cleanly

## Safety invariants preserved

- No stable public contract promoted.
- All v4.9 surfaces remain EXPERIMENTAL.
- No auto-apply, no auto-commit, no push.
- Non-TTY apply/commit print instructions only.
- TTY apply/commit require explicit `y` confirmation.
- No RAG, no embeddings, no vector DB, no LangGraph.
- No v5 scheduled.
