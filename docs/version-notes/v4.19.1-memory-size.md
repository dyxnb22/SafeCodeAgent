# v4.19.1: memory-size

## Base
- Depends on: `v4.19.0`
- Plan: `docs/version-plans/v4.19.1-memory-size.md`

## Completion Notes
- Added `sac memory size [--json]` (EXPERIMENTAL) to `src/safecode/cli_memory.py`.
- New pure helper module `src/safecode/memory/sizing.py` with `compute_sac_size(project_root)`:
  - Walks `.sac/` recursively via `Path.iterdir()`.
  - Skips symlinks and atomic temp files matching `r"^\..+\.[0-9a-f]{32}\.tmp$"`.
  - Returns `SacSizeReport` (Pydantic) with `root`, `exists`, `total_bytes`, `total_files`,
    `scopes` (sorted by name), `experimental: true`.
  - Named scopes: `audit`, `checkpoints`, `memory`, `runtime_logs`, `tasks`, `other`.
  - `tasks/budgets/` counted under `tasks`.
- JSON envelope uses `command="memory size"`, `status="success"` (stable contract 11).
- Human output: Rich Table titled `[EXPERIMENTAL] .sac/ Size Breakdown`.
- No file content read; no mutation; pure `Path.stat()` walk.
- No stable contract promoted.

## Tests
- `tests/test_memory_size.py`: 12 tests covering all 8 plan cases plus 3 CLI integration tests.

## Tag
- `v4.19.1`
