# v6.6.1 — eval-fixture-expansion

Evaluation evidence is broader and more explicit.

Changes:
- `src/safecode/eval/live.py`: default live eval fixtures now cover calculator
  fix, docs edit, multi-file refactor, test failure repair, and config schema
  migration with local success conditions.
- `tests/eval_fixtures/swebench_lite/`: synthetic inline SWE-bench Lite suite
  expands from 3 to 7 fixtures.
- `tests/snapshots/live_eval/baseline.json`: ratchets the default live fixture
  count to five fixtures.
- `tests/snapshots/swebench_lite/latest.json`: mock-provider baseline updated
  to honest 0/7.
- `docs/demo/eval-methodology.md`: documents fixture design principles,
  SWE-bench Lite compatibility scope, live-provider command, and benchmark
  caveats.

Validation:
- `PYTHONPATH=src python3 -m pytest tests/test_live_eval_mode.py tests/test_eval_swebench_adapter.py -q`
- `PYTHONPATH=src python3 -m safecode.cli eval --mode swebench-lite --suite tests/eval_fixtures/swebench_lite`
