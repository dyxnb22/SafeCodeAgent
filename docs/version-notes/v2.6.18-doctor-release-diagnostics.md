# v2.6.18 — Doctor Release Diagnostics

## Summary

Extends `sac doctor` with release diagnostics for version consistency, exact tag
state, documentation finalization, and preflight readiness.

## What changed

- `src/safecode/doctor.py`: adds `release_version`, `release_tag`,
  `release_docs`, and `release_preflight` checks.
- `tests/test_doctor_release_diagnostics.py`: focused Doctor and CLI coverage.
- `tests/test_release_version_bump.py`: fixes test isolation so bump-helper
  tests no longer mutate repository version assertions.

## Verification

```bash
PYTHONPATH=src python3 -m pytest tests/test_doctor_release_diagnostics.py tests/test_install_update_polish.py tests/test_release_version_bump.py -q
PYTHONPATH=src python3 -m pytest -q
```
