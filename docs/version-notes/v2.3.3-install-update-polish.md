# v2.3.3 — Install/Update Polish

## Summary

Improves install diagnostics, version reporting, and update guidance.

## Implementation

- `src/safecode/__init__.py` now matches package version metadata.
- `sac version` prints the SafeCode version and source-checkout update hint.
- `sac doctor` reports config file, `.sac/`, hook approval env, and sandbox approval env status.
- `docs/install-update.md` documents common install and update flows.

## Verification

```bash
PYTHONPATH=src python3 -m pytest tests/test_install_update_polish.py -q
PYTHONPATH=src python3 -m pytest -q
```
