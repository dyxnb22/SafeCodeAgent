# v3.99.1 - promotion-decisions

## Summary

- T-3.99.1-A promotion-decision-pass: updated `docs/public-contracts.md`
  with explicit v4.0 promotion decisions for each candidate surface.
- Extended `tests/test_public_contract_snapshots.py` with
  `TestV40PromotionDecisions` so the promote/defer/reject outcomes remain
  explicit.

## Decisions

| Candidate | Decision | Rationale |
|---|---|---|
| CLI `--json` envelope | Promote (already stable) | Stable since v3.7.2 with docs and snapshot. |
| MCP read execution | Promote (already stable) | Stable since v3.8.2 with docs, snapshot, and unchanged read-gate evidence. |
| IDE JSON-RPC | Defer | VSIX build and release-cycle consumption evidence are missing. |
| TUI interactive | Reject stable promotion at v4.0 | Frozen experimental; no stable automation output contract. |
| `sac report html` | Defer | Implemented and tested, but not snapshot-promoted. |
| Sandbox real-execution opt-in | Defer | Preflight/env gates exist, but backend pass evidence is host-local and not enough for a stable v4 contract. |

No new stable contract is promoted by v3.99.1 itself.

## Validation

- Targeted: `PYTHONPATH=src python3 -m pytest tests/test_public_contract_snapshots.py -q`
- Full suite: `PYTHONPATH=src python3 -m pytest -q`
- Release preflight: `PYTHONPATH=src python3 -m safecode.cli release preflight`
