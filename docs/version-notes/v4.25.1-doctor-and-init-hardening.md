# v4.25.1: doctor-and-init-hardening

## Base
- Depends on: `v4.25.0`
- Plan: `docs/version-plans/v4.20-to-v5.0-product-roadmap.md` (v4.25.1 section)

## Completion Notes
- `src/safecode/doctor.py`:
  - `Doctor._sac_dir_diagnostics()`: returns two new diagnostics (B14 fix):
    1. `sac_dir_writable`: touches `.sac/.doctor_probe` and deletes it; PASS on success, FAIL with hint on `PermissionError` or any exception; creates `.sac/` if missing.
    2. `disk_space`: `shutil.disk_usage(project_root).free`; PASS if ≥ 100 MB, WARN (not FAIL) if < 100 MB, SKIP if `disk_usage` raises.
  - `run_diagnostics()`: appends `_sac_dir_diagnostics()` to the standard diagnostic list.
- `src/safecode/cli_init.py`:
  - `_init_live_connectivity_check(provider, api_key, console_obj)`: B15 fix — module-level helper; calls `Doctor._live_anthropic_ping()` for `anthropic`, `Doctor._live_provider_ping()` for others; prints green "reachable" on PASS, yellow warning + "sac doctor --live" hint on FAIL, silent on SKIP; never raises (errors caught).
  - `run_init()`: calls `_init_live_connectivity_check()` after config write when `provider != "mock"` and `resolved_key` is set.

## Tests
- `tests/test_doctor_sac_dir.py` (8 tests, all pass):
  - `TestSacDirWritable` (4): writable PASS, PermissionError FAIL, hints present, .sac/ created
  - `TestDiskSpace` (4): plenty of space PASS, <100 MB WARN, disk_usage raises SKIP, run_diagnostics includes both
- `tests/test_cli_init_b15.py` (7 tests, all pass):
  - `TestInitLiveConnectivityCheck` (7): reachable/warning output, anthropic ping, openai ping, never raises, SKIP suppresses output, warning includes doctor hint

## Tag
- `v4.25.1`
