# v4.12.4 - FastAPI Todo Profile Tracking

v4.12.4 is a narrow tracked-artifact hotfix on top of the v4.12 resume-MVP cut.

## Changed

- `.gitignore` now keeps the repository-wide `.sac/` runtime state ignored while
  explicitly allowing `examples/fastapi-todo/.sac/project_profile.json`.
- `examples/fastapi-todo/.sac/project_profile.json` is tracked so the FastAPI
  todo demo has a clean-clone deterministic test profile.

## Notes

- No runtime behavior changed.
- No stable contract changed.
- No experimental surface was promoted.
- Package/runtime metadata still reports `4.12.3`; before any package release
  announcement, decide whether to run the release-bump/tag-update flow or treat
  `v4.12.4` as a repository hotfix tag only.
