# SafeCode Examples

Use `sac demo list` to inspect built-in workflows and `sac demo materialize WORKFLOW_ID` to create a local seed project.

Recommended onboarding workflows:

- `failing-test-repair`
- `fastapi-health-endpoint`
- `docs-safety-note`
- `safe-shell-status`
