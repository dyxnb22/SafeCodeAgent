# Python CLI Skill

Use this skill when the task is about a Python command line project.

Checklist:

- Read `pyproject.toml` first.
- Find the console script under `[project.scripts]`.
- Start code reading from the CLI entrypoint.
- Keep commands reviewable and auditable.

