# FastAPI Skill

Use this skill when the task is about a FastAPI project.

Checklist:

- Locate the app object, usually `app = FastAPI()`.
- Add small routes with clear response models where useful.
- Keep demo endpoints deterministic.
- Verify with a simple import or test before applying larger changes.

