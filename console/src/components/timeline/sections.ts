/** Timeline section metadata (mirrors render_markdown.py SECTION_ORDER). */

export { TIMELINE_SECTION_ORDER } from "@/lib/redaction/display";

export function sectionHeading(name: string): string {
  return name;
}
